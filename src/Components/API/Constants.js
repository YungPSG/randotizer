
// host that stores the images
export const IMAGE_BASE_URL = "https://cdn.cloudflare.steamstatic.com"

// API root
export const OPENDOTA_BASE_URL = "https://api.opendota.com/api/"

// within the API, this grabs the hero list as JSON
export const API_HERO_PATH = "heroStats"

export default { IMAGE_BASE_URL, OPENDOTA_BASE_URL, API_HERO_PATH }